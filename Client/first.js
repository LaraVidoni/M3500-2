/*$(document).ready(function() {
    var exempleSocket = new WebSocket("ws://127.0.0.1:8080/chat?pseudo=toto");
    console.log(exempleSocket);


    exempleSocket.onopen = function (event) {
        exempleSocket.send("Voici un texte que le serveur attend de recevoir dès que possible !");
    };
}) */

$chat-body;